# API
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Base de données
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Détection USB
pyudev==0.24.1

# Parsing PDF Afifly
pdfplumber==0.11.0

# Transfert fichiers caméras MTP/PTP (GoPro, Insta360, Sony, etc.)
gphoto2==2.5.0

# HTTP client (Open GoPro API via USB NCM)
requests==2.32.3

# Tâches planifiées (nettoyage rétention)
apscheduler==3.10.4

# Variables d'environnement
python-dotenv==1.0.1

# Sécurité (hachage des mots de passe + JWT)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1   # passlib 1.7.4 n'est pas compatible avec bcrypt 4.1+
python-jose[cryptography]==3.3.0

# Validation email (requis par Pydantic EmailStr)
email-validator==2.1.1

# Upload de fichiers dans FastAPI
python-multipart==0.0.9
